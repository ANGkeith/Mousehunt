version: '3.7'

x-base: &base
  image: mousehunt
  build: .
  shm_size: "3gb"
  devices:
    - /dev/snd
  environment:
    DISPLAY: "unix${DISPLAY}"
    PULSE_SERVER: unix:${XDG_RUNTIME_DIR}/pulse/native
    UID: 1000
    GID: 1000

services:
